//
//  ContentView.swift
//  FreeTube
//
//  Created by leshko on 17/5/26.
//
//  The original auto-generated entry view has been superseded by `App/RootView.swift`.
//  This file is left as a build-graph placeholder so the existing Xcode target reference does
//  not become a dangling pointer. It is not used at runtime. Remove the file reference (right-
//  click in Xcode → Delete → "Remove Reference") once you've added the new `App/` group.

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("FreeTube — see App/RootView.swift")
    }
}
